# Access Platform Design — Glossary

Terms used throughout this volume. Synthetic fixture content.

| Term | Definition |
| --- | --- |
| `artifact` | Pipeline deterministic orchestrate contract cache lint permission propagate downstream registry. |
| `baseline` | Render topology fixture contract deploy telemetry workflow telemetry lint deterministic serialize baseline; |
| `canonical` | Palette latency downstream heuristic reconcile reconcile immutable renovate lint cache baseline entropy. |
| `coverage` | Gateway annotate migrate architecture assertion lint deterministic artifact interface baseline deploy downstream latency; |
| `immutable` | Assertion throttle baseline propagate rollout reconcile pipeline throttle workflow module token baseline artifact pipeline topology downstream drift heuristic interface heuristic? |
| `interface` | Pipeline boundary canonical scope provision converge propagate deploy token manifest serialize config lint threshold? |
| `invariant` | Interface document workflow canonical coverage lint validate renovate invariant manifest converge permission deterministic observability deterministic entropy permission artifact manifest. |
| `pipeline` | Registry observability latency publish canonical coverage config idempotent serialize telemetry latency; |
| `render` | Ephemeral heuristic artifact artifact cache upstream propagate checksum drift. |
| `throttle` | Topology orchestrate render config template deterministic latency artifact topology workflow document contract migrate checksum manifest schema artifact entropy. |
| `throughput` | Manifest contract renovate canonical drift checksum deterministic system provision publish heuristic system throughput lint module observability interface deterministic document. |
| `token` | Provision telemetry cache token latency coverage validate module renovate config canonical lint upstream annotate telemetry baseline. |
| `upstream` | Validate workflow serialize registry checksum document workflow orchestrate schema immutable registry reconcile immutable palette baseline propagate assertion provision render? |
| `validate` | Publish coverage template immutable immutable rollout schema boundary converge checksum downstream ephemeral architecture latency contract. |
